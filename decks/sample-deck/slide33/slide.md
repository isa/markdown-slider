---
title: Mermaid — outline nodes
subtitle: mermaidNodes — outline (stroke-only boxes)
mermaidNodes: outline
---

```mermaid
flowchart LR
  A[Input] --> B[Process]
  B --> C[Output]
```

Compare with **filled** node style on another slide: set `mermaidNodes: filled` or omit (default).
