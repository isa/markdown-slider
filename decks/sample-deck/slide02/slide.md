---
title: Typography — headings & inline
subtitle: H1 lives in the body here; the deck header uses the YAML title
theme: default
---

# Heading one in the slide body

## Heading two

### Heading three

Regular paragraph with **strong**, *emphasis*, ***strong emphasis***, and `inline code`.

[Links open in a new tab](https://example.com) — useful for references.

<ins>Inserted text</ins> uses the HTML `<ins>` element (similar to an underline in many browsers).

Horizontal rule below.

---

~~Strikethrough~~ via GFM. Line break with two spaces at end  
works in markdown.

Subscripts and superscripts: H<sub>2</sub>O and E = mc<sup>2</sup> (raw HTML).
