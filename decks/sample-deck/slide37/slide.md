---
title: Working area — HTML lab
subtitle: Optional style.css and script.js are inlined at build time
---

The main slide is short; flip to the sandbox to try UI ideas. The iframe allows scripts (`sandbox` includes **allow-scripts**).
